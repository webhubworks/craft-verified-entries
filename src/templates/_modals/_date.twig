{% import "_includes/forms" as forms %}

{{ forms.dateField({
    label: 'Verified until'|t('verified-entries'),
    id: 'verifiedUntilDate',
    name: 'verifiedUntilDate',
    containerAttributes: {
        style: {
            width: '100%',
        }
    },
    value: now,
    required: true,
}) }}
